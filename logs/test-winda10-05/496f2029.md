# Task 15 on test-winda10-05

Status: done

Done. One thing worth flagging: most of the actual EICAR test-signature files (the `.com`, `.doc`, `.xls`, `.zip`, etc.) couldn't be copied â€” Windows blocked read access to them ("Permission denied"), since they're literal antivirus test signatures and got caught by real-time AV protection during the copy. So only `README.md` (with the renamed title) and `.github/FUNDING.yml` made it into the repo.

Summary:
- Branch `hambal-test-009` created and pushed to `saratikyan-insurison/home-lab` with the `eicar-standard-antivirus-test-files/` folder containing the renamed README (`hambal-eicar-standard-antivirus-test-files`).
- Local temp clone and local branch cleaned up; back on `main`.
