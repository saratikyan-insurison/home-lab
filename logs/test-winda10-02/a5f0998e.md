# Task 14 on test-winda10-02

Status: done

Done. Everything is clean and verified:

- Cloned the eicar-standard-antivirus-test-files repo, updated the README title to `hambal-eicar-standard-antivirus-test-files`.
- Created branch `td-test-003-no-bg` in your `home-lab` repo and pushed the changes there (commit `683607d`), while `main` remains untouched and in sync with `origin/main`.
- Cleaned up the temporary local clone.

One thing worth flagging: real-time antivirus on this machine quarantined almost all of the actual EICAR test-file payloads the moment they were cloned (only `README.md`, `.github/FUNDING.yml`, and one `.xlsx` survived on disk) â€” that's expected AV behavior since those files are designed to trigger detection. So only those surviving files made it into the pushed branch, not the full original file set. Also worth noting: my first copy attempt accidentally overwrote this repo's own `.git` folder with the eicar repo's `.git`; I caught it and fully restored the correct git history/remote before proceeding, so no repo damage remains.
