# Caldera Operation Report — `telegram-1784855357`

**Generated:** 2026-07-24
**Source:** Caldera server on `test-ubuntu-01` (`http://127.0.0.1:8888`), queried directly via the REST API (`/api/v2/operations`) because the built-in report/Debrief generator is currently crashing for this operation (see **Known Issue** below).

## Summary

| Field | Value |
|---|---|
| Operation ID | `cadaf1e1-91ea-42f9-85b7-e3a913337b21` |
| Name | `telegram-1784855357` |
| State | `running` (never reached `finished`) |
| Start | 2026-07-24T01:09:18Z |
| Last activity | 2026-07-24T01:42:11Z (~33 min of active tasking) |
| Adversary profile | "Everything Bagel" (`785baa02-df5d-450a-ab3a-1a863f22b4b0`) — a superset profile containing effectively all stockpile abilities |
| Planner | atomic |
| Group | red |
| Obfuscator | plain-text |
| Autonomous | yes |
| Jitter | 2/8 |
| Visibility | 50 |

## Targets

The operation's host group contained 3 agents, but only one ever executed anything:

| Paw | Host | User | Privilege | Trusted | Last seen | Activity |
|---|---|---|---|---|---|---|
| `hxrtfp` | test-winda10-06 | `TEST-WINDA10-06\test-winda10-01` | Elevated | yes | 2026-07-24T01:42:11Z | **All 122 links ran here** |
| `xifodz` | test-winda10-02 | `TEST-WINDA10-02\test-winda10-01` | User | no | 2026-07-24T00:27:25Z | none — untrusted/stale, never tasked |
| `vxkrdr` | test-winda10-03 | `TEST-WINDA10-03\test-winda10-01` | User | no | 2026-07-24T00:27:14Z | none — untrusted/stale, never tasked |

The two untrusted agents last checked in ~40 minutes before this operation started and were excluded from tasking, so effectively this was a single-host run against `test-winda10-06`.

## Results

122 abilities were attempted against `test-winda10-06`:

| Status | Count | % |
|---|---|---|
| SUCCESS | 51 | 42% |
| ERROR | 67 | 55% |
| TIMEOUT | 4 | 3% |

### Breakdown by tactic

| Tactic | Total | Success | Error | Timeout |
|---|---|---|---|---|
| initial-access | 4 | 2 | 2 | 0 |
| collection | 29 | 10 | 19 | 0 |
| execution | 76 | 33 | 39 | 4 |
| stealth | 13 | 6 | 7 | 0 |

Execution and collection abilities dominated the operation; discovery/lateral-movement/privilege-escalation/exfiltration tactics were not exercised in this run (the adversary profile has abilities for them, but none were selected/decided upon by the atomic planner in this window).

### Notable successes

- **Initial access (simulated):** `T1566.001` — "Download Macro-Enabled Phishing Attachment" and "Word spawned a command shell and used an IP address in the command line" both succeeded, i.e. the emulated phishing/macro execution chain completed on the target.
- **Execution:** commodity techniques succeeded repeatedly — command-shell scripting (`T1059.001`/`T1059.003`), WMI execution and remote-service discovery (`T1047`), and click-fix / macro-style execution (`T1204.002`).
- **Stealth:** successful anti-forensics/defense-evasion steps included WMIC XSL bypass (`T1220`, local and remote variants), HTML smuggling (`T1027.006`), hidden-file creation via `attrib` (`T1564.001`), and single-file deletion via `cmd` (`T1070.004`).
- **Collection:** successful clipboard capture (`T1115`), screen capture (`T1113`), webcam/microphone registry artifacts (`T1125`/`T1123`), and general recon-for-exfil commands (`T1119`) all ran cleanly.

### Failure pattern

The majority of failures were concentrated in more specialized/OS-hardened techniques that don't apply cleanly to this target's configuration — e.g. archiving/compression-for-exfiltration abilities requiring tools not present (7zip, WinRAR, WinZip, Rar, Makecab), ESXi-specific abilities (irrelevant on a Windows endpoint), Cobalt Strike named-pipe emulation, PsExec-based lateral movement, and several credential/AD-recon abilities (SOAPHound, BloodHound-from-memory). These read as expected environment-mismatch failures rather than defensive blocks — there's no evidence of AV/EDR detection messages in the link output, just missing prerequisites (binaries, elevated context for certain calls, etc.).

### Facts collected

1,519 facts were learned during the operation, almost entirely `host.file.path` (1,516) generated by process/window-enumeration abilities (e.g. `WMIC bypass` walking the running-process list) — mostly OS/Edge/Office housekeeping process paths, not attacker-relevant artifacts. The remaining 3 facts were `host.ip.address` = `192.168.10.19`, which is the Caldera server's own C2 IP (agent beacon config), not a discovered target.

No credential material, domain facts, or lateral-movement targets were collected in this run.

## Known issue: report generation is broken for this operation

The Caldera server log (`caldera/logs/systemd.log`) shows the built-in report generator crashing twice while producing the report for this operation:

```
2026-07-24 05:34:35 ERROR  Error generating operation report (telegram-1784855357)
...
JSONDecodeError: Invalid \escape: line 1 column 52227 (char 52226)
```

This happens in `app/objects/c_operation.py:338`, where a link's raw command output is `json.loads()`'d before being appended to the report — one of the 122 links' output apparently contains an unescaped backslash sequence that breaks the parser. The same bug also hit a separate operation (`hambal-002`) at 05:34:28. This means the operation currently cannot produce a report through the UI/Debrief plugin; the numbers in this report were reconstructed directly from the `/api/v2/operations/<id>` payload instead. Recommend filing/patching this as a bug in `c_operation.py`'s `report()` method (should tolerate malformed JSON in step output rather than raising).

## Conclusion

`telegram-1784855357` was a broad, single-host atomic-planner run of the "Everything Bagel" adversary profile against `test-winda10-06`. It never formally reached a `finished` state, has no explicit "success" objective evaluation output (report generation errors out), and produced a fairly typical mixed result for a kitchen-sink ability set: about 42% of attempted techniques succeeded, concentrated in generic execution/collection/stealth actions, with most failures attributable to missing tooling or non-applicable ability requirements on this host rather than active defense.
